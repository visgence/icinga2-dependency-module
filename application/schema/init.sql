CREATE TABLE node_positions (
    node_name TEXT,
    node_x INTEGER,
    node_y INTEGER 
);


CREATE TABLE plugin_settings (
    api_endpoint TEXT,
    api_user TEXT,
    api_password TEXT 
);

CREATE TABLE graph_settings (
    setting_name TEXT,
    setting_value TEXT,
    setting_type TEXT
);

