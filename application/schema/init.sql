CREATE TABLE node_positions (
    node_name TEXT,
    node_x INTEGER,
    node_y INTEGER 
);


CREATE TABLE plugin_settings (
    database_resource TEXT,
    api_endpoint TEXT,
    api_user TEXT,
    api_password TEXT 
);